[package]
name = "lau-state-machine"
version = "0.1.0"
edition = "2021"
description = "Hierarchical state machine for agents and game entities — deterministic transitions, guards, actions"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
